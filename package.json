{
  "scripts": {
    "predev": "concurrently  \"cd authserver && yarn add all\" \"cd backend && yarn add all\"  \"cd socketio && yarn add all\"",
    "dev": " concurrently  \"cd authserver && yarn run dev\" \"cd backend && yarn run dev\"  \"cd socketio && yarn run dev\""
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
